# WebTasks
Manage a todo list

### Instructions to run
Make sure you have `npm` and `node` installed. Then, run `npm install` to install required dependencies.

Run `npm run build` to transpile the react code. Finally, run `npm run app` to start the program.

